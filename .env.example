# Copy to .env and fill in as needed.

# Full path (or PATH-resolvable name) of the hayabusa binary to invoke.
# If unset, falls back to `hayabusa`/`hayabusa.exe` on PATH.
# See scripts/download_hayabusa.py for a way to fetch this.
HAYABUSA_BIN=./hayabusa/hayabusa.exe
